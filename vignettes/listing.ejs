```{=html}
<div class="list gallery">
    <% for (const item of items) { %>
    <div class="gallery-entry" <%= metadataAttrs(item) %>>
        <div class="metadata">          
            <p class="date listing-date"><%= item.date %></p>
        </div>
        <div class="body">
            <p class="title listing-title"><a href="<%- item.path %>"><%= item.title %></a></p>
            <% if (item.categories) { %>
            <div class="item-categories listing-categories">
                <% for (const category of item.categories) { %>
                <div class="item-category listing-category" onclick="window.quartoListingCategory('<%=category%>'); return false;">
                    <%= category %>
                </div>
                <% } %>
            </div>
            <% } %>
            <p class="author listing-author"><%= item.author %></p>
            <p class="description listing-description"><%= item.description %></p>
            <ul class="item-links">
                <% if (item.links) { %>
                    <% for (const link of item.links) { %>
                    <li class="item-link">
                        <% if (link.local) { %>
                        <a href="<%- item.path.substring(0, item.path.lastIndexOf('/')) %>/<%- link.url %>">
                        <% } else { %>
                        <a href="<%- link.url %>">
                        <% } %>
                            <i class="<%- link.icon %>"></i> <%= link.name %>
                        </a>
                    </li>
                    <% } %>
                <% } %>
            </ul>
        </div>
        <div class="thumbnail">
            <% if (item.url) { %>
            <a href="<%= item.url %>" target="_blank"><img src="<%= item.image %>" alt="<%= item.title %>" title="<%= item.title %>" /></a>
            <% } else { %>
            <img src="<%= item.image %>" alt="<%= item.title %>" title="<%= item.title %>" />
            <% } %>
        </div>
        
    </div>
    <% } %>
</div>
```